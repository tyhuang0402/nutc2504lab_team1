# Kubernetes 核心概念速成指南
## 專為「算力管理與多租戶」任務設計

> 目標：10天內理解足夠的 K8s 概念來完成測試任務

---

## 🎯 你需要理解的核心概念（按優先級）

### 1. **Namespace（命名空間）**⭐⭐⭐
**為什麼重要**：這是實現多租戶隔離的第一層機制

**簡單理解**：
```
K8s Cluster（整個機房）
├── namespace: tenant-a（公司A的專屬區域）
│   ├── Pod: llm-service-1
│   └── Pod: llm-service-2
├── namespace: tenant-b（公司B的專屬區域）
│   └── Pod: llm-service-1
└── namespace: monitoring（監控系統的區域）
    └── Pod: prometheus
```

**關鍵指令**：
```bash
# 建立 namespace
kubectl create namespace tenant-a

# 查看所有 namespace
kubectl get namespaces

# 在特定 namespace 操作
kubectl get pods -n tenant-a
```

**多租戶應用**：
- 每個租戶一個 namespace
- 用 ResourceQuota 限制每個 namespace 的資源使用
- 用 NetworkPolicy 隔離網路流量

---

### 2. **Pod（容器組）**⭐⭐⭐
**為什麼重要**：這是 K8s 中最小的部署單位，你的 LLM 服務會跑在 Pod 裡

**簡單理解**：
```
Pod = 一個或多個緊密關聯的容器 + 共享的網路和儲存

例如：
┌─────────────────────────┐
│   Pod: llm-service      │
│  ┌──────────────────┐   │
│  │  Container:      │   │
│  │  LiteLLM Proxy   │   │
│  └──────────────────┘   │
│  共享 IP: 10.0.1.5      │
└─────────────────────────┘
```

**關鍵 YAML 範例**：
```yaml
apiVersion: v1
kind: Pod
metadata:
  name: litellm-proxy
  namespace: tenant-a
  labels:
    app: llm-service
    tenant: tenant-a
spec:
  containers:
  - name: litellm
    image: ghcr.io/berriai/litellm:main-latest
    resources:
      limits:
        nvidia.com/gpu: 1  # 限制使用 1 個 GPU
      requests:
        memory: "4Gi"
        cpu: "2"
```

---

### 3. **Service（服務）**⭐⭐
**為什麼重要**：讓你的 Pod 可以被穩定存取（即使 Pod 重啟 IP 也不變）

**簡單理解**：
```
Service 就像是「櫃台號碼」，背後的 Pod 像「工作人員」

          ┌──────────────┐
客戶請求 → │   Service    │ → 自動分配到健康的 Pod
          │ (固定 IP)    │
          └──────────────┘
               ↓  ↓  ↓
          ┌────┐┌────┐┌────┐
          │Pod1││Pod2││Pod3│
          └────┘└────┘└────┘
```

**關鍵類型**：
- **ClusterIP**（預設）：只能在叢集內部存取
- **NodePort**：開放節點的某個 port 給外部存取
- **LoadBalancer**：整合雲端供應商的負載均衡器

---

### 4. **Deployment（部署）**⭐⭐⭐
**為什麼重要**：自動管理 Pod 的生命週期（擴展、更新、自癒）

**簡單理解**：
```
Deployment = 「我要 3 個這樣的 Pod，確保它們一直運行」

如果 Pod 掛了 → Deployment 自動重啟
如果要更新版本 → Deployment 逐步替換（Rolling Update）
如果要擴展 → kubectl scale deployment llm-service --replicas=5
```

**關鍵 YAML 範例**：
```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: litellm-deployment
  namespace: tenant-a
spec:
  replicas: 2  # 要運行 2 個 Pod 副本
  selector:
    matchLabels:
      app: llm-service
  template:  # 這裡定義 Pod 的規格
    metadata:
      labels:
        app: llm-service
    spec:
      containers:
      - name: litellm
        image: ghcr.io/berriai/litellm:main-latest
        ports:
        - containerPort: 8000
```

---

### 5. **ResourceQuota（資源配額）**⭐⭐⭐
**為什麼重要**：這是「算力配額」的核心實作機制

**簡單理解**：
```
ResourceQuota = 每個 namespace 的「資源預算」

例如：tenant-a 最多只能用：
- 4 個 GPU
- 16GB 記憶體
- 8 個 CPU 核心
```

**關鍵 YAML 範例**：
```yaml
apiVersion: v1
kind: ResourceQuota
metadata:
  name: tenant-a-quota
  namespace: tenant-a
spec:
  hard:
    requests.nvidia.com/gpu: "2"  # 最多申請 2 個 GPU
    requests.memory: "16Gi"
    requests.cpu: "8"
    limits.memory: "32Gi"
    pods: "10"  # 最多 10 個 Pod
```

**監控方式**：
```bash
kubectl describe quota -n tenant-a
# 會顯示：已使用 / 配額上限
```

---

### 6. **ConfigMap & Secret（配置管理）**⭐⭐
**為什麼重要**：儲存 API Key、配置檔等資訊

**簡單理解**：
```
ConfigMap = 明文配置（例如：LiteLLM 的 config.yaml）
Secret = 加密配置（例如：OpenAI API Key）
```

**實際範例**：
```yaml
# ConfigMap: LiteLLM 配置
apiVersion: v1
kind: ConfigMap
metadata:
  name: litellm-config
  namespace: tenant-a
data:
  config.yaml: |
    model_list:
      - model_name: gpt-4
        litellm_params:
          model: openai/gpt-4
          api_key: os.environ/OPENAI_API_KEY

---
# Secret: API Key
apiVersion: v1
kind: Secret
metadata:
  name: openai-secret
  namespace: tenant-a
type: Opaque
data:
  OPENAI_API_KEY: c2stZXhhbXBsZWtleQ==  # base64 編碼
```

---

## 🏗️ 多租戶架構概念圖

```
┌─────────────────────────────────────────────────────────────┐
│                    Kubernetes Cluster                        │
│                                                              │
│  ┌─────────────────────┐      ┌─────────────────────┐      │
│  │  Namespace: tenant-a │      │  Namespace: tenant-b │      │
│  │  ┌─────────────────┐│      │  ┌─────────────────┐│      │
│  │  │ ResourceQuota:  ││      │  │ ResourceQuota:  ││      │
│  │  │ - 2 GPUs        ││      │  │ - 1 GPU         ││      │
│  │  │ - 16Gi RAM      ││      │  │ - 8Gi RAM       ││      │
│  │  └─────────────────┘│      │  └─────────────────┘│      │
│  │                     │      │                     │      │
│  │  ┌─────────────────┐│      │  ┌─────────────────┐│      │
│  │  │ Deployment:     ││      │  │ Deployment:     ││      │
│  │  │ litellm-service ││      │  │ litellm-service ││      │
│  │  │   └─ Pod 1      ││      │  │   └─ Pod 1      ││      │
│  │  │   └─ Pod 2      ││      │  │                 ││      │
│  │  └─────────────────┘│      │  └─────────────────┘│      │
│  │                     │      │                     │      │
│  │  ┌─────────────────┐│      │  ┌─────────────────┐│      │
│  │  │ Service (API)   ││      │  │ Service (API)   ││      │
│  │  │ ClusterIP       ││      │  │ ClusterIP       ││      │
│  │  └─────────────────┘│      │  └─────────────────┘│      │
│  └──────────┬──────────┘      └──────────┬──────────┘      │
│             │                            │                 │
│  ┌──────────┴────────────────────────────┴──────────┐      │
│  │         Ingress / API Gateway                    │      │
│  │         (Cilium / Nginx / Kong)                  │      │
│  │  - 路由: /tenant-a/* → tenant-a namespace        │      │
│  │  - Rate Limiting: 1000 req/hr per tenant        │      │
│  │  - JWT 驗證                                      │      │
│  └──────────────────────────────────────────────────┘      │
│             ↓                                               │
│  ┌──────────────────────────────────────────────────┐      │
│  │     Monitoring (Prometheus + Grafana)            │      │
│  │  - DCGM (GPU metrics)                            │      │
│  │  - API 流量統計                                   │      │
│  │  - 按租戶分類的儀表板                             │      │
│  └──────────────────────────────────────────────────┘      │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎓 你的任務映射到 K8s 概念

| 任務需求 | K8s 實作方式 |
|---------|-------------|
| **多租戶隔離** | 每個租戶一個 Namespace + NetworkPolicy |
| **算力配額** | ResourceQuota（限制 GPU/CPU/Memory） |
| **API 流量控管** | Ingress（Cilium/Nginx）+ Rate Limiting |
| **LLM 服務部署** | Deployment（運行 LiteLLM Pods） |
| **GPU 監控** | DCGM Exporter → Prometheus → Grafana |
| **租戶自動建立** | Kubernetes API（用 Python/Go 寫腳本） |

---

## 📖 推薦學習路徑（10天計畫）

### Day 1-2: 基礎環境
- [ ] 安裝本地 K8s（Minikube/Kind/Docker Desktop）
- [ ] 熟悉 `kubectl` 基本指令
- [ ] 練習建立 Namespace、Pod、Deployment

### Day 3-4: 多租戶原型
- [ ] 建立 2 個 tenant namespaces
- [ ] 設定 ResourceQuota
- [ ] 部署簡單的 nginx 或 LiteLLM

### Day 5-6: API Gateway
- [ ] 安裝 Nginx Ingress（或研究 Cilium）
- [ ] 設定路由規則（/tenant-a → namespace tenant-a）
- [ ] 測試 Rate Limiting

### Day 7-8: 監控整合
- [ ] 部署 Prometheus
- [ ] 研究 DCGM Exporter（即使沒真 GPU）
- [ ] 建立 Grafana Dashboard 設計

### Day 9-10: 文檔撰寫
- [ ] 架構設計圖
- [ ] API 規格（OpenAPI/Swagger）
- [ ] 安全性與 Air-gapped 部署方案

---

## 🔑 關鍵理解檢查點

在開始實作前，確保你能回答：

1. **多租戶如何實現？**
   → 用 Namespace 隔離 + ResourceQuota 限制資源

2. **如何限制某個租戶只能用 2 個 GPU？**
   → 在該租戶的 namespace 設定 ResourceQuota

3. **API Gateway 的作用是什麼？**
   → 路由流量到正確的租戶、驗證身份、限制流量

4. **如果租戶的 Pod 掛了怎麼辦？**
   → Deployment 會自動重啟

5. **如何知道哪個租戶用了多少 GPU？**
   → Prometheus 抓取 DCGM metrics + 用租戶的 label 分類

---

## 💡 實用資源

- [K8s 官方互動教學](https://kubernetes.io/docs/tutorials/kubernetes-basics/)
- [LiteLLM Proxy 文檔](https://docs.litellm.ai/docs/proxy/quick_start)
- [NVIDIA DCGM Exporter](https://github.com/NVIDIA/dcgm-exporter)
- [Cilium 多租戶指南](https://docs.cilium.io/en/stable/)

---

**下一步**：安裝本地 K8s 環境並跑第一個 Pod！
